
version: 0.2
run-as: root

phases:

  install:
    commands:
      - pip install archerysec-cli --force  
      - echo $(PWD)

    
  post_build:
    commands:
      - docker run --net zapnet -v $(pwd):/zap/wrk/:rw -t owasp/zap2docker-stable zap-full-scan.py -I -j -m 10 -T 60 -t http://ab8ecdb44a07e45cca1762a090b1e15f-1198928900.ap-south-1.elb.amazonaws.com -r 20210417-zap-full-scan-without-user.html
      
